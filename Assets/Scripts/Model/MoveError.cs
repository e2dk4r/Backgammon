public enum MoveError
{
    // no error
    NoError,
    // there is not enough step value to move in
    NotEnoughSteps,
    // unknown error
    Unknown,
    BlockedByEnemy,
    WrongHomeDirection,
}
